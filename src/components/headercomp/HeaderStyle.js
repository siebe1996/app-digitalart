import { StyleSheet } from "react-native";

export const headerStyle = StyleSheet.create({
    title: {
        fontSize: 16,
        fontWeight: "bold",
    },
    subtitle: {
        fontSize: 12,
        color: "gray",
    },
});
