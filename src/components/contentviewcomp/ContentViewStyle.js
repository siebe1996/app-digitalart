import { StyleSheet } from "react-native";

export const contentViewStyle = StyleSheet.create({
    container: {
        marginTop: 10,
        maxWidth: 500,
        minWidth: 300,
        alignSelf: "center",
    },
});
